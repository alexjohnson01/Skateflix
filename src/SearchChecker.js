/*const searchChecker = (arr) => {
    for (let i = 0; i < arr.length; i++) {
      let arrName = arr[i].name;
      let arrId = arr[i].id;
      let similarity = compareTwoStrings(val, arrName);
      if (similarity > 0.1) {
        if (!idArray.includes(arrId)) {
          setSearchArray([...searchArray, arr[i]]);
          setIdArray(...idArray, arrId);
        }
      }
    }
    if (searchArray.length === 0) {
      setSearchResults(false);
    } else {
      setSearchResults(true);
    }
  }; 
  */